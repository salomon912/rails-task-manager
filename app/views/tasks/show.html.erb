<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed %></p>


<%= link_to "Go back to tasks", tasks_path %>
<%= link_to "Update task", update_path %>
<%= link_to "Delete task", destroy_path,
method: "delete", data: { confirm: "Are you sure?" }%>
